# MinePaint

It is a minimal clone of MyPaint that uses the brushlib library in the backend. It is written in C++ and has as few features as possible. It only has drawing, erasing, zooming, and panning features. You can select brush settings and color from the right panel. 

# Demo
![example](https://github.com/ehanhalici/minepaint/blob/master/minePaint.png)

# Install
If you download brushlib as dependencies/libmypaint and run libmypaint_compile.sh, it will be compiled for static linking. Then run compile.sh. The program size will be less than 1MB. The ldd output is extremely small.

